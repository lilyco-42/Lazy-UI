-- 循环提示词
-- 需求
- P0 
    - imgui的 组件实现
- p1 
    - md3 风格化设计 
    - kotlin compose 
- p2 
    - 约定大于配置  
    - 默认组件最优



- 当你无法准确理解某些词语的含义,可在下方获取详细解释
    - imgui : 指https://github.com/ocornut/imgui C++ 写的UI 风格和各种组件与后端无关
    - md3 : https://m3.material.io/ 指Material Design 3 是谷歌的开源设计系统，用于构建美观、易用的产品。我需要的其色彩设计和合适的ui大小.
    - kotlin compose : 指一种自描述函数参数和ui函数 设计方法,我需要ply 原本链式调用样式封装为 目标样式,以便后续目标.代码具有自描述性 fn box(height: 100px,width:100px);
    - 约定大于配置 : 
     1.rail on ruby 的思想 
    {具体实现:当我声明一个按钮时,确保它有以下默认样式:跨平台兼容,合适的ui和字体.
    我只需绑定两个变量:文本,函数.}

    详细例子:{当我使用button("启动",a()); fn a() {print("pressed") 绑定函数a(),按钮自动传递数据给需要的函数. 或是button("hello") 代表一个组件,无函数绑定,但仍然可以通过默认生成id组件号获取. 为了快速绘出组件.默认的样式都应是默认最优软件设计规范是如何: }}
    - 默认组件最优: 默认的组件已经处理好i18n,ui跨平台渲染问题,默认组件.默认你是一个优先的开发者,默认是最优模板.{
    main(){
    侧边栏({
    启动()
    设置()
    关于()
    })
    日志面板()
    日志进度条(默认=nvim dialog样式)
    }
    }比如这样开发ui,开发者引入 侧边栏.rs 和 侧边栏.toml rs文件实现了ui,toml文件实现了最优最常见配置,侧边栏和日志面板关系.toml 文件自动推断样式(侧边栏 与日志面板)应为flex,自动推断日志进度条在底部 10%.等
