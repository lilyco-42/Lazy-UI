# Log progress bar — convention over configuration.
# nvim-dialog style: a slim progress track pinned to the bottom.

height = 48        # total bar height in px
track_ratio = 0.10 # progress track thickness as a fraction of height
gap = 4
padding = 8
