# Sidebar layout — convention over configuration.
# Only override what you need; every field already has an optimal M3 default
# baked into src/components/config.rs. Width/size comes from app_layout.toml.

gap = 4            # spacing between nav items
padding = 12       # inner padding in px
scroll = true      # scroll when content overflows
